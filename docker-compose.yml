version: "3.9"
services:
  failing:
    image: ghcr.io/rubenrubens/cq-testing:demo
    volumes:
    - .:/testing
    command: bash failing.sh
  passing:
    image: ghcr.io/rubenrubens/cq-testing:demo
    volumes:
    - .:/testing
    command: bash passing.sh
